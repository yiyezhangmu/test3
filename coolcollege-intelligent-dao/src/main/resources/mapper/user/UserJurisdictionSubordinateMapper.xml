<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.coolcollege.intelligent.dao.user.UserJurisdictionSubordinateMapper">

    <insert id="batchInsertUserJurisdictionSubordinate" parameterType="com.coolcollege.intelligent.model.user.UserJurisdictionSubordinateDO" >
        insert into user_jurisdiction_Subordinate_${eid}
        (
         user_id,
         underling_id
         )
        values
        <foreach collection="items" item="item" separator=",">
            (
             #{item.userId},
             #{item.underlingId}
             )
        </foreach>
    </insert>

    <delete id="deleteUserJurisdictionSubordinateByUserId">
        delete from user_jurisdiction_Subordinate_${enterpriseId}
               where user_id in
                <foreach collection="userIds" item="id" open="(" close=")" separator=",">
                    #{id, jdbcType=VARCHAR}
                </foreach>

    </delete>

</mapper>